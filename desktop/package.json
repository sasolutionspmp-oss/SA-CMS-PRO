{
  "name": "sa-cms-pro-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "concurrently -n frontend,electron -c cyan,magenta \"npm run frontend:dev\" \"npm run electron:dev\"",
    "frontend:dev": "npm --prefix ../frontend run dev",
    "electron:dev": "wait-on tcp:5173 && cross-env NODE_ENV=development VITE_DEV_SERVER_URL=http://localhost:5173 electron .",
    "build": "npm --prefix ../frontend run build",
    "start": "npm run prod",
    "prod": "npm run build && cross-env NODE_ENV=production electron ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.3",
    "electron": "^32.1.2",
    "wait-on": "^7.0.1"
  }
}
